services:
  db:
    image: postgis/postgis:17-3.5
    environment:
      POSTGRES_USER: tripchive
      POSTGRES_PASSWORD: tripchive
      POSTGRES_DB: tripchive
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
